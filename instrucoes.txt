**code . no (gitbash)

vue create projectname

delete hello world component
create new components
import components on App 
add the new components to export default
add the new components to template
npm run serve to update the application
Download boostrap vue (npm i bootstrap-vue boostrap)

 Imports:
import BootstrapVue from 'bootstrap-vue'
	Vue.use(BootstrapVue)

import 'bootstrap/dist/css/bootstrap.css'
import 'bootstrap-vue/dist/bootstrap-vue.css'

adicionei component nav do boostrap
npm run serve
*ajuste dos componentes*
*gerando call da api*
adicionando function mounted ao export default com fatch chamando api com method get 
e then com response mostrando no log por json